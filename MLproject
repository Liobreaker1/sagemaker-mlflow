name: housing-price-project

entry_points:
  train:
    parameters:
      model: {type: str, default: "elasticnet"}
      experiment: {type: str, default: "housing-price-exp"}
    command: >
      python src/train.py
      --model {model}
      --experiment {experiment}
